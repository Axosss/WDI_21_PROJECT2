<h1>Homepage</h1>

<!-- This is not working -->
<div class="row">
  <% @recipes.each do |recipe| %>
    <div class="col-md-6">
      <div class="card">
        <% if !recipe.recipe_image.blank? %>
          <%= link_to image_tag(recipe.recipe_image.url), recipe_path(recipe), :class => 'card-img-top' %>
        <% else %>
          <img src="http://www.fillmurray.com/300/300" class="recipe-index">
        <% end %>
      </div>
    </div>
  <% end %>
</div>
